<nav class="flex gap-1 mb-6 border-b border-gray-200">
  <%= link_to "Overview", club_path(club),
      class: "px-4 py-2 text-sm font-medium rounded-t #{current_page?(club_path(club)) ? 'bg-white border border-b-white border-gray-200 -mb-px text-gray-900' : 'text-gray-500 hover:text-gray-700'}" %>
  <%= link_to "Grade Levels", club_grade_levels_path(club),
      class: "px-4 py-2 text-sm font-medium rounded-t #{current_page?(club_grade_levels_path(club)) ? 'bg-white border border-b-white border-gray-200 -mb-px text-gray-900' : 'text-gray-500 hover:text-gray-700'}" %>
  <%= link_to "Courts", club_courts_path(club),
      class: "px-4 py-2 text-sm font-medium rounded-t #{current_page?(club_courts_path(club)) ? 'bg-white border border-b-white border-gray-200 -mb-px text-gray-900' : 'text-gray-500 hover:text-gray-700'}" %>
  <%= link_to "Players", club_players_path(club),
      class: "px-4 py-2 text-sm font-medium rounded-t #{current_page?(club_players_path(club)) ? 'bg-white border border-b-white border-gray-200 -mb-px text-gray-900' : 'text-gray-500 hover:text-gray-700'}" %>
</nav>
