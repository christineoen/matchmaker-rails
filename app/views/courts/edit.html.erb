<div class="w-full max-w-2xl">
  <h1 class="font-bold text-3xl mb-1"><%= @club.name %></h1>
  <p class="text-sm text-gray-500 mb-6">Club setup</p>

  <%= render "shared/club_setup_nav", club: @club %>

  <h2 class="text-lg font-semibold text-gray-900 mb-4">Edit court</h2>

  <%= render "form", club: @club, court: @court %>

  <div class="mt-4">
    <%= link_to "Cancel", club_courts_path(@club), class: "text-sm text-gray-500 hover:text-gray-700" %>
  </div>
</div>
