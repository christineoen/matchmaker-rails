<%= render "shared/errors", object: court %>

<%= form_with model: [ club, court ] do |f| %>
  <div class="flex gap-3 items-end">
    <div class="w-28">
      <%= f.label :name, "Name", class: "block text-sm font-medium text-gray-700 mb-1" %>
      <%= f.text_field :name, required: true, placeholder: "1, H1, …",
          class: "block w-full shadow-sm rounded-md border border-gray-300 focus:outline-blue-600 px-3 py-2" %>
    </div>
    <div class="flex-1">
      <%= f.label :surface, "Surface", class: "block text-sm font-medium text-gray-700 mb-1" %>
      <%= f.text_field :surface, required: true, placeholder: "grass, hard, clay",
          class: "block w-full shadow-sm rounded-md border border-gray-300 focus:outline-blue-600 px-3 py-2" %>
    </div>
<%= f.submit class: "px-4 py-2 bg-blue-600 hover:bg-blue-500 text-white text-sm font-medium rounded-md cursor-pointer" %>
  </div>
<% end %>
